import { NgModule } from "@angular/core";

// Este módulo foi consolidado em order-general.module.ts
// Mantido apenas para compatibilidade com importações existentes

@NgModule({
  // Declarações e importações foram movidas para OrderGeneralModule
})
export class HeaderModule { }